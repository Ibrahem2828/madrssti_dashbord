"use client"; import {usePortalSession} from "@/providers/auth-provider"; export function Can({permission,anyOf,allOf,children}:{permission?:string;anyOf?:readonly string[];allOf?:readonly string[];children:React.ReactNode}){const access=usePortalSession();const allowed=(permission?access.can(permission):true)&&(anyOf?access.canAny(anyOf):true)&&(allOf?access.canAll(allOf):true);return allowed?<>{children}</>:null;}
