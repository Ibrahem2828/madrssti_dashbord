import {getTranslations} from "next-intl/server"; import {LoginForm} from "@/components/auth/login-form"; export default async function CentralLogin(){const t=await getTranslations("auth");return <main className="mx-auto grid min-h-screen max-w-md place-items-center p-6"><section className="w-full rounded-lg border bg-card p-6"><h1 className="text-2xl font-bold">{t("centralTitle")}</h1><p className="mb-6 mt-2 text-sm text-muted-foreground">{t("centralDescription")}</p><LoginForm portal="central"/></section></main>}
