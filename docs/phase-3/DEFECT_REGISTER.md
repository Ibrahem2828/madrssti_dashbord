# Phase 3 defect register

## Status legend

- Open
- In progress
- Resolved
- Deferred external blocker

## Defects

| ID | Severity | Symptom | Root cause | Files changed | Fix | Verification | Final status |
|---|---|---|---|---|---|---|---|
| P3-FE-001 | Critical | `/en/central` failed with a Next.js server/client serialization error. | Navigation config passed Lucide component functions across the server/client boundary. | `src/config/navigation.central.ts`, `src/config/navigation.school.ts`, `src/components/layout/portal-shell.tsx` | Replaced function-valued icons with serializable string keys and resolved icons only inside the client shell. | `npm run build` PASS, unauthenticated `/en/central` now returns a safe redirect instead of a runtime crash. | Resolved |
| P3-FE-002 | High | Lint/runtime correctness issues from unstable hooks and stale dependencies. | Several screens used unstable callbacks and incomplete effect dependencies. | Multiple Central and School screen components | Normalized loaders with `useCallback` and completed dependency arrays. | `npm run lint` PASS. | Resolved |
| P3-FE-003 | High | Production build failed during server env evaluation. | `NEXT_PUBLIC_APP_URL` validation executed eagerly even for routes that only needed `API_BASE_URL`. | `src/config/env.server.ts` | Made server env reads lazy per field. | `npm run build` PASS. | Resolved |
| P3-FE-004 | High | Local production runtime failed because standalone start did not load `.env.local`. | `npm start` launched standalone output without local env loading. | `scripts/start-standalone.mjs`, `package.json` | Added a standalone-aware local start wrapper that loads `.env.local` without overwriting explicitly supplied environment values. | `npm run start` PASS, `/api/health` -> `200`. | Resolved |
| P3-FE-005 | Critical | Auth failures could be misclassified and auth timeouts were not centrally controlled. | Timeout logic and HTTP status mapping were duplicated across login, health, refresh, gateway, and verifier code paths. | `src/lib/api/backend-core.js`, `src/lib/api/backend-core.d.ts`, `src/lib/api/backend.ts`, `src/features/auth/services/login-server.ts`, `src/app/api/health/backend/route.ts`, `src/app/api/auth/refresh/route.ts`, `src/app/api/auth/logout/route.ts`, `src/app/api/auth/school/switch-school/route.ts`, `src/lib/api/gateway.ts`, `src/lib/auth/session.ts`, `scripts/verify-backend-connectivity.mjs`, `tests/auth-bff.test.mjs` | Added shared timeout policy, shared transport classifier, shared HTTP status mapping, structured safe transport logging, and normalized invalid-backend-response handling. | `npm run typecheck` PASS, `npm run lint` PASS, `npm test` PASS (`13/13`). Invalid Central and School login now return `504 BACKEND_TIMEOUT` with request IDs when backend transport times out, not `403`. | Resolved |
| P3-SEC-001 | Critical | Login route protection needed explicit runtime proof after transport changes. | BFF login protection needed end-to-end confirmation for Origin and CSRF handling after auth transport refactor. | `src/features/auth/services/login-server.ts`, `src/components/auth/login-form.tsx`, `tests/auth-bff.test.mjs` | Enforced Origin + CSRF on login, kept cookie-only auth, and verified structured protection errors. | `ORIGIN_MISSING` -> `403`, `ORIGIN_NOT_ALLOWED` -> `403`, `CSRF_TOKEN_MISSING` -> `403`, `CSRF_TOKEN_MISMATCH` -> `403`, valid Origin + valid CSRF reaches backend layer and returns backend transport result. | Resolved |
| P3-ENV-001 | Critical | `/api/health/backend`, direct `node fetch`, and BFF login all time out against the real backend origin from the current Windows host. | The current host path to `https://api.madrasti.xn--mgbaab0cxheq.tech` resolves DNS and opens TCP 443, but HTTPS requests still time out or reset before a backend HTTP response is received. The active interface is `ProtonVPN`. | No frontend source change can resolve this. Evidence captured in docs and runtime logs. | Confirmed that the frontend helper and direct native fetch now fail the same way; this is no longer a BFF URL-construction or status-mapping defect. | `/api/health/backend` -> `502` with root and login probes classified as `BACKEND_TIMEOUT`; direct `node -e fetch(...)` -> `UND_ERR_CONNECT_TIMEOUT`; `curl.exe` -> connection reset; `Invoke-WebRequest` -> timeout. | Deferred external blocker |
| P3-OPS-001 | High | Docker runtime gate could not be executed. | Docker CLI exists, but the Docker Desktop Linux engine pipe is unavailable on this host. | No source change required. | Inspected `Dockerfile`; build/run are blocked by daemon availability rather than by application code. | `docker --version` PASS, `docker build` BLOCKED with `dockerDesktopLinuxEngine` pipe unavailable. | Deferred external blocker |